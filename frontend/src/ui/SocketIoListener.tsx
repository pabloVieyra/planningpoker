import { useEffect } from "react";
import { useContext } from "./Context";

const SocketIoListener: React.FC = () => {
    const { addNewMember } = useContext();

    useEffect(() => {
        
    }, []);

    return <></>
}

export default SocketIoListener
